name: iOS — Deploy to App Store

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'ios/**'
      - '.github/workflows/ios-deploy.yml'

jobs:
  deploy:
    name: Build & Submit to App Store
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler: 'latest'
          working-directory: ios

      - name: Install Fastlane
        working-directory: ios
        run: bundle install

      - name: Patch Fastlane OpenSSL compatibility
        working-directory: ios
        run: |
          FASTLANE_DIR=$(bundle exec ruby -e "puts Gem::Specification.find_by_name('fastlane').gem_dir")
          TOKEN_RB="$FASTLANE_DIR/spaceship/lib/spaceship/connect_api/token.rb"
          sed -i '' 's/OpenSSL::PKey::EC\.new/OpenSSL::PKey.read/g' "$TOKEN_RB"
          echo "Patched $TOKEN_RB"

      - name: Write App Store Connect API Key
        env:
          ASC_API_KEY_CONTENT: ${{ secrets.ASC_API_KEY_CONTENT }}
        run: |
          mkdir -p ~/asc-keys
          # Use printf to preserve newlines; replace literal \n with real newlines in case secret was stored that way
          printf '%s' "$ASC_API_KEY_CONTENT" | sed 's/\\n/\n/g' > ~/asc-keys/AuthKey_3349GWYVGR.p8
          echo "Key file first line: $(head -1 ~/asc-keys/AuthKey_3349GWYVGR.p8)"
          echo "Key file line count: $(wc -l < ~/asc-keys/AuthKey_3349GWYVGR.p8)"

      - name: Run Fastlane release
        working-directory: ios
        env:
          ASC_KEY_FILEPATH: ~/asc-keys/AuthKey_3349GWYVGR.p8
        run: bundle exec fastlane release
